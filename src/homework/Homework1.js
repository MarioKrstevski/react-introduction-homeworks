// Finish the components

function Button(props) {
  // https://react-bootstrap.github.io/components/buttons/
  // variant (string) defines the colors
  // outlined (boolean) - makes the button look outlined
  // size (string) - changes the fontSize
  // type code here
}

function Translate(props) {
  // language (string) - choose two languages
  // text - Text to translate
  // Define a small collection of translations, Hello, Goodbye, I like programming, React is awesome
}

function WelcomeUser(params) {
  // name (string)
  // if name is missing or is empty, it should say: <p> Hello user
  // if name is present, it should say: <h3> Hello {name}
  // if name is your name, it should say: <h1> Hello Master
}
